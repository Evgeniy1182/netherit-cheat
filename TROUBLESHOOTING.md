# 🔧 Troubleshooting Guide - Netherit Injector

## ❌ Проблемы с инжектом DLL

### 1. "Failed to open process" (Не удалось открыть процесс)

**Причины:**
- ❗ Нет прав администратора (самая частая причина)
- Процесс защищен системой (например, system процессы)
- Антивирус блокирует доступ

**Решение:**
```
1. Закройте приложение
2. Кликните правой кнопкой на NetheritInjector.exe
3. Выберите "Запуск от имени администратора"
4. Попробуйте снова
```

### 2. "Failed to create remote thread" (Не удалось создать удаленный поток)

**Причины:**
- ⚠️ Несовпадение архитектуры (32-bit vs 64-bit)
  - Если инжектор 64-bit, он НЕ МОЖЕТ инжектить в 32-bit процесс
  - Если инжектор 32-bit, он НЕ МОЖЕТ инжектить в 64-bit процесс
- Антивирус блокирует
- Процесс имеет защиту

**Решение:**
```
1. Проверьте архитектуру целевого процесса в Task Manager
2. Используйте инжектор той же архитектуры
3. Временно отключите антивирус (ОСТОРОЖНО!)
4. Добавьте инжектор в исключения антивируса
```

### 3. "LoadLibrary returned NULL" (Загрузка DLL не удалась)

**Причины:**
- DLL файл поврежден или невалидный
- Отсутствуют зависимости DLL
- DLL не совместима с процессом (32/64 bit)
- Путь содержит кириллицу или спецсимволы

**Решение:**
```
1. Проверьте, что DLL валидна (откройте в Dependency Walker)
2. Убедитесь, что все зависимости DLL присутствуют
3. Проверьте архитектуру DLL (должна совпадать с процессом)
4. Используйте путь без кириллицы (например, C:\inject\my.dll)
5. Убедитесь, что DLL имеет DllMain функцию
```

### 4. Антивирус блокирует

**Если Windows Defender блокирует:**
```
1. Откройте "Безопасность Windows"
2. Защита от вирусов и угроз → Управление настройками
3. Добавьте папку проекта в "Исключения"
```

## 🎯 Как правильно тестировать

### Тестовая DLL (C++)
Создайте простую DLL для тестирования:

```cpp
// test.cpp
#include <windows.h>

BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved) {
    if (ul_reason_for_call == DLL_PROCESS_ATTACH) {
        MessageBoxW(NULL, L"DLL Injected Successfully!", L"Test", MB_OK);
    }
    return TRUE;
}
```

Компилируйте:
```bash
cl /LD test.cpp /link user32.lib
```

### Безопасный тестовый процесс
Используйте notepad.exe для тестирования:
```
1. Запустите notepad.exe
2. Выберите его в инжекторе
3. Инжектите тестовую DLL
4. Должно появиться сообщение "DLL Injected Successfully!"
```

## 📊 Диагностика проблем

### Коды ошибок Windows (Error Codes)

| Код | Описание | Решение |
|-----|----------|---------|
| 5 | Access Denied | Запустите от имени администратора |
| 87 | Invalid Parameter | Проверьте путь к DLL |
| 127 | Procedure Not Found | Отсутствуют зависимости DLL |
| 998 | Invalid Access to Memory | Несовпадение архитектуры |

### Проверка архитектуры

**Проверка процесса:**
```
1. Откройте Task Manager (Ctrl+Shift+Esc)
2. Вкладка "Подробности" (Details)
3. Кликните правой кнопкой на заголовок столбца
4. Выберите столбцы → Платформа
5. Увидите: 32-bit или 64-bit
```

**Проверка DLL:**
```powershell
dumpbin /headers your.dll | findstr machine
# x86 = 32-bit
# x64 = 64-bit
```

## ⚠️ Важные ограничения

1. **Защищенные процессы** - не могут быть инжектированы:
   - System процессы
   - Процессы с защитой от записи
   - Игры с античитом (EAC, BattlEye и т.д.)

2. **Архитектура**:
   - 64-bit инжектор → только 64-bit процессы
   - 32-bit инжектор → только 32-bit процессы

3. **Права**:
   - Всегда нужны права администратора
   - UAC должен быть включен

## 🛡️ Безопасность

⚠️ **Легальное использование:**
- Тестирование собственных приложений
- Моддинг однопользовательских игр
- Разработка инструментов

❌ **Незаконное использование:**
- Читы для онлайн игр
- Взлом чужих приложений
- Обход защиты

## 💡 Советы

1. **Всегда тестируйте на notepad.exe сначала**
2. **Проверяйте архитектуру** перед инжектом
3. **Используйте простую DLL** для тестирования
4. **Смотрите коды ошибок** - они очень информативны
5. **Читайте сообщения об ошибках** - там детальная информация

## 📞 Поддержка

Если проблема не решена:
1. Запишите точный текст ошибки
2. Проверьте архитектуру процесса и DLL
3. Убедитесь, что запущено от имени администратора
4. Попробуйте с notepad.exe и тестовой DLL

---

**Удачного инжекта! 🚀**
