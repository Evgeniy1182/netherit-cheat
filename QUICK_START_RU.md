# 🚀 Быстрый старт - Netherit Injector

## 🔑 Авторизация

При запуске потребуется ввести ключ лицензии:
**Ключ:** `NETHERIT`

## Запуск

1. **Откройте** `bin\Release\net8.0-windows\NetheritInjector.exe`
2. **Кликните правой кнопкой** → "Запуск от имени администратора" ⚠️ ОБЯЗАТЕЛЬНО!
3. Введите ключ и наслаждайтесь снежинками ❄️

## Использование

### Шаг 1: Выбор процесса
```
Кнопка "Select Process" → Выберите целевой процесс → "Select"
```

### Шаг 2: Выбор DLL
```
Кнопка "Browse DLL" → Выберите .dll файл → "Открыть"
```

### Шаг 3: Инжект
```
Кнопка "⚡ INJECT DLL ⚡" (становится активной после выбора)
```

## ✅ Успешный результат

После инжекта увидите сообщение:
```
✓ DLL successfully injected!
Module handle: 0x7FF...
```

## ❌ Возможные ошибки

### "Not running as Administrator"
**Решение:** Закройте и запустите от имени администратора

### "Failed to open process" (Error: 5)
**Решение:** Запустите от имени администратора

### "Failed to create remote thread" (Error: 998)
**Причина:** Несовпадение архитектуры
**Решение:** 
- Проверьте в Task Manager: процесс 32-bit или 64-bit?
- Инжектор должен быть той же архитектуры!

### "LoadLibrary returned NULL"
**Возможные причины:**
1. DLL повреждена
2. Отсутствуют зависимости
3. Неправильная архитектура DLL
4. Путь с кириллицей

**Решение:**
- Используйте простой путь: `C:\test\my.dll`
- Проверьте DLL в Dependency Walker
- Убедитесь, что DLL той же архитектуры, что и процесс

## 🧪 Тестирование

### Создание тестовой DLL

1. Создайте файл `test.cpp`:
```cpp
#include <windows.h>

BOOL APIENTRY DllMain(HMODULE hModule, DWORD ul_reason_for_call, LPVOID lpReserved) {
    if (ul_reason_for_call == DLL_PROCESS_ATTACH) {
        MessageBoxW(NULL, L"DLL работает!", L"Успех", MB_OK);
    }
    return TRUE;
}
```

2. Скомпилируйте (нужен Visual Studio или MinGW):
```bash
# Visual Studio
cl /LD test.cpp /link user32.lib

# MinGW
gcc -shared -o test.dll test.cpp -luser32
```

3. Запустите `notepad.exe`
4. Инжектите `test.dll` в notepad
5. Должно появиться окно "DLL работает!"

## 📋 Чеклист перед инжектом

- [ ] Запущено от имени администратора
- [ ] DLL существует и валидна
- [ ] Архитектура совпадает (оба 32-bit или оба 64-bit)
- [ ] Процесс не защищен (не system процесс)
- [ ] Антивирус не блокирует
- [ ] Путь к DLL без спецсимволов

## 💡 Советы

1. **Для новичков:** Начните с notepad.exe + простая тестовая DLL
2. **Проверка архитектуры:** Task Manager → Details → Platform (столбец)
3. **Безопасность:** Тестируйте только свои программы!

## 🆘 Нужна помощь?

Подробная инструкция: [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

---

**Удачи! 🎉**
